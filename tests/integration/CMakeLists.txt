# tests/integration/CMakeLists.txt

# Legacy source location for integration scenarios.
# Target registration is now split by intent:
# - functional tests are registered in tests/functional/CMakeLists.txt
# - system tests are registered in tests/system/CMakeLists.txt
